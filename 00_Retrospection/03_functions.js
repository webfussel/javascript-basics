/**
 * Was ist eine Funktion?
 * Es gibt mehrere Arten Funktionen zu definieren.
 * Welche sind es und wo liegen die Unterschiede?
 */

function something (name) {
    return 'hallo ' + name
}

const arrow = name => 'hallo ' + name

const another = function () {
    return 123
}
